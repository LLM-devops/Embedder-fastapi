At least once, you need to specify embedding model in config and run the code to save the model locally.
- This way, the model parameters are included in the docker image with the environment.
- The code can check for the model parameters.